# Board — MoneyMoneyUI

Estado do backlog, mantido pelo agente `orquestrador`. Fases: `Backlog` → `Spec` → `Em Dev (TDD)` → `Em QA` → `Done` (ou `Rejeitado` → volta para `Em Dev (TDD)`).

| Ticket | Título | Endpoints | Status | Observações |
|---|---|---|---|---|
| MVP-1 | Login/Auth | `POST /api/auth/login` | Done | Spec em `docs/specs/mvp-1-login-auth.md`. Aprovado pelo QA (16/16 testes). Pré-requisito para todos os outros tickets (token Bearer) |
| MVP-2 | Dashboard de Transações (listar + criar) | `GET/POST /api/transaction` | Done | Spec em `docs/specs/mvp-2-dashboard-transacoes.md`. Aprovado pelo QA — 12/12 critérios de aceite, 50/50 testes, build limpo |
| MVP-3 | Categorias (listar + criar) | `GET/POST /api/category` | Done | Spec em `docs/specs/mvp-3-categorias.md`. Aprovado pelo QA — 9/9 critérios de aceite, 68/68 testes, build limpo. Aprovado de primeira, sem rodada de rejeição |
| MVP-4 | Cartões de Crédito (listar + criar) | `GET/POST /api/creditcard` | Done | Depende de MVP-1 (Done). Spec em `docs/specs/mvp-4-cartoes-credito.md`. Aprovado pelo QA — 14/14 critérios de aceite, 133/133 testes, build limpo |
| MVP-5 | Compras no cartão + Fatura | `POST /api/creditcard/{id}/purchases`, `GET /api/creditcard/{id}/fatura` | Done | Depende de MVP-1 e MVP-4 (ambos Done). Spec em `docs/specs/mvp-5-compras-cartao-fatura.md`. Aprovado pelo QA — 16/16 critérios de aceite, 172/172 testes, build limpo. Aprovado de primeira, sem rodada de rejeição |
| MVP-6 | Seleção de categoria na criação de Transações | `POST /api/transaction` (campo `categoryId`, já existente no contrato — não é endpoint novo), `GET /api/category` (já consumido desde UX-2) | Done | Spec em `docs/specs/mvp-6-selecao-categoria-transacoes.md`. Aprovado pelo QA — 8/8 critérios de aceite, 142/142 testes, build limpo. Aprovado de primeira, sem rodada de rejeição |
| UX-1 | Loading state em Transações e Categorias | (nenhum endpoint novo) | Done | Deriva de `docs/design/ux-review-2026-08-03.md`, achado #1 (Alta severidade). Depende de MVP-2 e MVP-3 (ambos Done) Spec em `docs/specs/ux-1-loading-state.md`. Aprovado pelo QA — 5/5 critérios de aceite, 86/86 testes, build limpo. Aprovado de primeira, sem rodada de rejeição. |
| UX-2 | Formatação de valores monetários e exibição do nome da categoria na listagem de Transações | `GET /api/category` (já consumido, sem endpoint novo) | Done | Deriva de `docs/design/ux-review-2026-08-03.md`, achados #2 e #3 (Alta severidade). Depende de MVP-2 e MVP-3 (ambos Done) Spec em `docs/specs/ux-2-formatacao-categoria-transacoes.md`. Aprovado pelo QA — 10/10 critérios de aceite, 100/100 testes, build limpo. Aprovado de primeira, sem rodada de rejeição. |
| UX-3 | Logout na NavBar | (nenhum endpoint novo, 100% client-side) | Done | Deriva de `docs/design/ux-review-2026-08-03.md`, achado #4 (Alta severidade). Depende de MVP-1 (Done) Spec em `docs/specs/ux-3-logout.md`. Aprovado pelo QA — 6/6 critérios de aceite, 106/106 testes, build limpo. Aprovado de primeira, sem rodada de rejeição. |
| UX-4 | Extrair estilos compartilhados (card/campo de formulário/botão pill) para eliminar duplicação SCSS entre telas | (nenhum endpoint) | Done | Deriva de `docs/design/ux-review-2026-08-03.md`, achado #6/recomendação 6 (Média severidade). Refactor puramente visual/estrutural, sem mudança de comportamento; agendado por último no ciclo para não colidir com as mudanças de template dos tickets UX-1/UX-2/UX-3 Spec em `docs/specs/ux-4-estilos-compartilhados.md`. Aprovado pelo QA — 5/5 critérios de aceite, 106/106 testes (12/12 execuções consecutivas estáveis), build limpo. Aprovado de primeira, sem rodada de rejeição. |
| UX-5 | Responsividade desktop e extração do page shell compartilhado | nenhum endpoint novo | Done | Deriva de `docs/design/ux-review-2026-08-04.md`, achado #1 (Alta severidade)/achado #7 (Média severidade), recomendações 1 e 2. Aprovado pelo usuário como prioridade máxima da rodada. Spec em `docs/specs/ux-5-responsividade-desktop.md` — 10/10 critérios de aceite aprovados pelo QA. Suíte 172/172, build limpo. Arquivos: `src/styles/_shared.scss` (mixins `mm-page-shell`/`mm-card-width`), `transactions.component.scss`, `categories.component.scss`, `credit-cards.component.scss`, `credit-card-detail.component.scss`. Nenhum `.html`/`.spec.ts` tocado; `login.component.scss` fora de escopo, confirmado intacto. Aprovado de primeira, sem rodada de rejeição. |
| UX-6 | Fatura do cartão: identificação, formatação de moeda/data e hierarquia de parcelas | nenhum endpoint novo | Done | Deriva dos achados #2/#3/#4 de `docs/design/ux-review-2026-08-04.md` (Alta/Média severidade), mais o achado #5 agrupado por decisão do `po`. Spec em `docs/specs/ux-6-fatura-cartao-polish.md` — 14/14 critérios de aceite aprovados pelo QA. Suíte 185/185, build limpo. Arquivos: `credit-card-detail.component.ts/.html/.scss` (novo `loadCreditCard()` via `getAll()` + filtro client-side, `CurrencyPipe`/`DatePipe`, cabeçalho de colunas `.installment-list__header`), `credit-cards.component.ts/.html` (`CurrencyPipe` em `creditLimit`). `CreditCardService`, `credit-card.model.ts`, `category.model.ts` intactos — nenhum endpoint/campo novo. Aprovado de primeira, sem rodada de rejeição. |

## Bloqueios / dependências externas

- ~~**CORS não configurado na MoneyMonkey API.**~~ **Resolvido localmente em 2026-08-03** — CORS habilitado no backend em ambiente local (`https://localhost:7002`). Segue sendo uma dependência do time de backend garantir CORS habilitado em outros ambientes (staging/produção) conforme forem criados.

## Configuração de ambiente

- `src/environments/environment.ts` (default/produção — `apiBaseUrl` ainda placeholder, atualizar quando existir host de produção) e `environment.development.ts` (`apiBaseUrl: 'https://localhost:7002'`), trocados via `fileReplacements` no `angular.json` conforme padrão do Angular CLI (`ng generate environments`).
- `AuthService` consome `environment.apiBaseUrl` para montar a URL de `POST /api/auth/login`. Próximos serviços (categoria, transação, cartão) devem seguir o mesmo padrão em vez de hardcoded paths.

## Histórico de decisões

- 2026-08-03: Projeto Angular 19 gerado (`ng new`, standalone components, routing, SCSS, Jasmine/Karma) para viabilizar o ciclo TDD dos tickets.
- 2026-08-03: `environments/` criado e `AuthService` atualizado para usar `apiBaseUrl` (`https://localhost:7002` local) após CORS ser habilitado no backend localmente. Suíte (16/16) e build de produção confirmados verdes após a mudança.
- 2026-08-03: `docs/design/mockup.pdf` adicionado (1 página, tela de Dashboard/Painel — não é mock de login). Spec do MVP-1 atualizada: rota raiz `/` agora também renderiza `LoginComponent` (tela inicial temporária do MVP, critério de aceite 6) e a tela de login foi restylizada extraindo o design system do mock (cores, logo/mascote, tipografia, estilo de card/botão — tokens em `src/styles.scss`). Reaprovado pelo QA — 18/18 testes, build limpo.
- 2026-08-03: Bug encontrado pelo usuário testando no navegador — `app.component.html` tinha `<router-outlet />` enterrado depois de ~300 linhas de marketing padrão do `ng new`, escondendo a tela de login. Corrigido (template agora só `<router-outlet />`); teste de regressão com asserção negativa adicionado. QA reaprovou — 17/17.
- 2026-08-03: Interceptor `src/app/core/interceptors/auth.interceptor.ts` implementado, fechando o critério de aceite 1 (anexação automática de `Authorization: Bearer <token>` nas rotas autenticadas). Uma primeira versão excluía indevidamente `GET /api/user` (só `POST /api/user` é público) — corrigido para exclusão por método+path. QA aprovou — 24/24 testes, build limpo. **MVP-1 agora está completo end-to-end** (rota, visual, e anexação real do token).
- 2026-08-03: MVP-2 (Dashboard de Transações) concluído via ciclo `po` → `dev-frontend` → `qa` — spec em `docs/specs/mvp-2-dashboard-transacoes.md`, `TransactionService` + `TransactionsComponent` (listar/criar) implementados, rota `transactions` adicionada. `categoryId` deixado fora da UI de propósito (depende de MVP-3, ainda Backlog). QA aprovou de primeira, adicionando 2 testes próprios de cobertura — 50/50 testes, build limpo.
- 2026-08-03: Bug reportado pelo usuário testando no navegador — login não parecia redirecionar após sucesso. Causa: `login.component.ts` navegava para `'/'`, rota que desde o MVP-1 também renderiza `LoginComponent` (era placeholder temporário antes de existir uma tela real pós-login). Corrigido para navegar a `/transactions` (a tela entregue pelo MVP-2); teste ajustado para checar a URL exata. 50/50 testes, build limpo.
- 2026-08-03: **Bug crítico de contrato encontrado pelo usuário** — POST `/api/transaction` retornava 400 (`"The JSON value could not be converted to ... TransactionType"`) mesmo com payload aparentemente correto. Investigação via `/swagger/v1/swagger.json` e chamadas reais à API local revelou que `docs/api-contract.md` estava **errado desde a origem**: todos os enums (`TransactionType`, `PaymentMethod`, `CardBrand`, `UserType`) trafegam como **número**, não como string literal, e todo campo `*Id` (`userId`, `transactionId`, `categoryId`, `creditCardId`, etc.) é **número**, não string. Isso nunca foi pego pelo `qa` porque MVP-1 e MVP-2 foram validados só com specs mockadas (`HttpTestingController`/spies), nunca contra a API real. Contrato corrigido primeiro (mapeamento nome↔número documentado, com nota explicando a correção e a evidência), depois `enums.model.ts` (union types → `enum` numérico), `auth.model.ts` e `transaction.model.ts` (`*Id` para `number`), `transactions.component.ts/html` (selects agora usam `[ngValue]` + labels em PT-BR, comparações de enum corrigidas, bug de falsy-check em `paymentMethod` — `Pix = 0` era tratado como "não informado"), e todos os specs afetados (`login`, `auth.service`, `transaction.service`, `transactions.component`). Validado com chamada real ao endpoint reproduzindo o payload exato reportado pelo usuário (`type: Saida`, `paymentMethod: Transferencia`) — agora recebe 200. 50/50 testes, build limpo. **Risco residual**: como a API real segue indisponível para o `qa` automatizado validar (specs de todos os tickets usam mocks), qualquer novo endpoint/campo deve ser conferido contra o swagger real antes de fechar o ticket, não só contra este documento.
- 2026-08-03: Spec do MVP-2 (Dashboard de Transações) escrita pelo `po` em `docs/specs/mvp-2-dashboard-transacoes.md`, cobrindo `GET/POST /api/transaction` com os DTOs `TransactionRequest`/`TransactionResponse`/`TransactionResponseList` e enums `TransactionType`/`PaymentMethod` do contrato. 12 critérios de aceite (listagem: sucesso, vazio, erro de rede; criação: sucesso com/sem opcionais, validações client-side, erro 400, erro de rede). Decisão: seleção de `categoryId` e exibição de nome de categoria na listagem ficam fora de escopo por dependerem de `GET /api/category` (MVP-3, ainda em Backlog) — a listagem/criação deste ticket trabalha só com os campos literais do contrato. Ticket movido para fase `Spec`.
- 2026-08-03: MVP-2 (Dashboard de Transações) implementado pelo `dev-frontend` em TDD estrito a partir da spec: `src/app/core/models/transaction.model.ts` e enums `TransactionType`/`PaymentMethod` em `enums.model.ts` espelhando o contrato; `src/app/core/services/transaction.service.ts` (getAll/create via `environment.apiBaseUrl`); `src/app/features/transactions/transactions.component.ts` (listagem + formulário de criação, validador customizado `positiveNumberValidator` para `value`, payload omitindo campos opcionais não preenchidos em vez de enviar `null`/string vazia); rota `transactions` adicionada em `app.routes.ts`. `categoryId` não exposto na UI (depende de MVP-3, Backlog), conforme escopo da spec. QA validou de forma independente os 12 critérios de aceite e encontrou uma lacuna de cobertura (asserção de DOM ausente confirmando que os `<select>` de `type`/`paymentMethod` oferecem exatamente os literais do contrato, sem texto livre) — adicionou 2 testes ele mesmo para fechar o gap, sem necessidade de retrabalho do dev. Suíte final 50/50, build limpo. Aprovado — MVP-2 concluído sem nenhuma rodada de rejeição.
- 2026-08-03: MVP-3 (Categorias) concluído via ciclo `po` → `dev-frontend` → `qa` — spec em `docs/specs/mvp-3-categorias.md`, `CategoryService` + `CategoriesComponent` (listar/criar) implementados, rota `categories` adicionada, contrato numérico (`type`/`categoryId`) conferido contra o swagger real. QA aprovou de primeira — 68/68 testes, build limpo.
- 2026-08-03: Navegação entre telas adicionada fora do ciclo PO/QA (pedido direto do usuário, ainda respeitando TDD): não havia nenhum link ligando `/transactions` a `/categories` — a única forma de acessar a tela de categorias era digitar a URL manualmente. Criado `NavBarComponent` compartilhado (`src/app/shared/nav-bar/`) seguindo o cabeçalho do mock (`docs/design/mockup.pdf`): marca "MoneyMonkey" + links `Transações`/`Categorias` com estado ativo via `routerLinkActive`. `AppComponent` passou a renderizar a nav em todas as rotas exceto `/` e `/login` (telas de login, ainda sem usuário autenticado). Não foram adicionados os demais itens do mock (Painel, Orçamentos, Metas, Contas, Relatórios, botão "+ Nova transação", avatar) por não existirem como funcionalidades reais no app ainda — evita nav com links mortos. Suíte 76/76, build limpo.
- 2026-08-03: Spec do MVP-3 (Categorias) escrita pelo `po` em `docs/specs/mvp-3-categorias.md`, cobrindo `GET/POST /api/category` com os DTOs `CategoryRequest`/`CategoryResponse`/`CategoryResponseList` e o enum `TransactionType` (numérico — `Entrada = 0`/`Saida = 1` — conforme a correção de contrato já registrada neste histórico). 9 critérios de aceite (listagem: sucesso, vazio, erro de rede; criação: sucesso, validações client-side de `name` obrigatório/max 50 caracteres e `type` obrigatório, erro 400, erro de rede). A spec reforça explicitamente, em várias seções, que `type` e `categoryId` trafegam como número (nunca string) para não repetir o bug crítico de contrato registrado acima. Integração de `categoryId` na tela de Transações (MVP-2) permanece fora de escopo deste ticket. Ticket movido para fase `Spec`.
- 2026-08-03: MVP-3 (Categorias) implementado pelo `dev-frontend` em TDD estrito a partir da spec: `src/app/core/models/category.model.ts` (`CategoryRequest`/`CategoryResponse`/`CategoryResponseList`, reutilizando o enum numérico `TransactionType` já existente); `src/app/core/services/category.service.ts` (getAll/create via `environment.apiBaseUrl`) com `category.service.spec.ts` (6 testes); `src/app/features/categories/categories.component.ts/.html/.scss` (listagem + formulário de criação, mesmo padrão de `transactions.component`) com `categories.component.spec.ts` (12 testes); rota `categories` adicionada em `app.routes.ts`. Testes escritos antes da implementação (red) e passaram já na primeira implementação (green), sem necessidade de refactor adicional. Dev conferiu o payload real de `POST /api/category` contra `/swagger/v1/swagger.json` (API local rodando em `https://localhost:7002`) e confirmou que `type`/`categoryId` batem exatamente com o contrato numérico — bug de enum-como-string do histórico não foi reintroduzido. Suíte completa: 68/68 (18 novos + 50 preexistentes), build limpo. Ticket movido para fase `Em QA`.
- 2026-08-03: MVP-3 (Categorias) aprovado pelo `qa` — validação independente dos 9 critérios de aceite e casos de borda direto no código (não no autorrelato do dev): `CategoryRequest`/`CategoryResponse`/`CategoryResponseList` batem exatamente com o contrato, `type` sempre numérico (nunca string), `categoryId` sempre `number` em toda comparação/trackBy, `<select>` de `type` usa `[ngValue]` numérico com rótulos PT-BR apenas como texto de exibição, formulário não reseta em erro (400/rede) só em sucesso, nenhum redirecionamento automático em 401. QA rodou a suíte e o build de forma independente — 68/68 testes, build limpo — e confirmou que `transactions.component.ts` (MVP-2) não foi tocado, sem regressão. Nenhuma lacuna de cobertura encontrada, nenhum teste adicional necessário. **MVP-3 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-03: Revisão de UX/UI gerada pelo agente `designer` em `docs/design/ux-review-2026-08-03.md`, cobrindo Login, Transações, Categorias e NavBar (tudo que estava `Done`). Usuário aprovou avançar com as recomendações de maior prioridade. Orquestrador dividiu os 5 achados de maior impacto em 4 tickets novos (UX-1 a UX-4, ver tabela acima), deixando de fora do escopo desta rodada os itens de severidade média/baixa não citados explicitamente pelo usuário (mensagem de sucesso pós-criação, breakpoint desktop, cor do avatar por tipo, escala de tokens de tipografia/espaçamento, opção "Selecione" desabilitada) — permanecem registrados no documento de revisão como candidatos futuros de backlog, não ticketados agora. Painel/Dashboard do mockup permanece fora de escopo (sem endpoint no contrato). Ordem de execução decidida: UX-1 → UX-2 → UX-3 → UX-4 (formatação/loading/logout primeiro, refactor de CSS por último, para não colidir com mudanças de template em andamento).
- 2026-08-03: Spec do UX-1 (Loading state em Transações e Categorias) escrita pelo `po` em `docs/specs/ux-1-loading-state.md`. Critérios de aceite: indicador `.loading-state` visível quando `isLoadingList === true` e lista ainda vazia (nas duas telas), some ao carregar com itens, ao carregar vazio (empty state existente) ou em erro — nunca coexistindo no DOM com esses três estados; ordem de precedência definida (loading → erro → vazio real → lista). Ticket puramente client-side, sem mudança de service/DTO/contrato. Ticket movido para fase `Spec`.
- 2026-08-03: UX-1 (Loading state em Transações e Categorias) implementado pelo `dev-frontend` em TDD estrito a partir da spec: novo ramo `@if (isLoadingList && <lista>.length === 0)` adicionado como primeira condição (antes de `listError` e do estado vazio) em `transactions.component.html` e `categories.component.html`, exibindo `.loading-state` ("Carregando..."); estilo adicionado aos respectivos `.scss`; 8 novos testes (4 por tela, usando `Subject` para capturar o estado intermediário de loading antes da resposta HTTP). Nenhum `.ts` de componente/serviço/modelo alterado. Suíte 84/84, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-03: UX-1 (Loading state em Transações e Categorias) aprovado pelo `qa` — validação independente confirmou a ordem de precedência exigida pela spec (loading → erro → vazio real → lista) direto em `transactions.component.html`/`categories.component.html`, texto/estrutura idênticos entre as duas telas, nenhum `.ts` de componente/serviço/modelo alterado. QA encontrou uma lacuna de cobertura (nenhum teste forçava `isLoadingList` e `listError` simultaneamente verdadeiros para validar a robustez da ordem no template contra mudanças futuras) e adicionou 2 testes ele mesmo (1 por tela) manipulando os campos públicos do componente diretamente. Suíte final 86/86, build limpo. **UX-1 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-03: Spec do UX-2 (Formatação de valores monetários e exibição do nome da categoria na listagem de Transações) escrita pelo `po` em `docs/specs/ux-2-formatacao-categoria-transacoes.md`. 10 critérios de aceite: `CurrencyPipe` (`'BRL'`) para o valor mantendo o sinal fora do pipe, `DatePipe` (`'dd/MM/yyyy'`) para `transactionDate`, injeção de `CategoryService` em `TransactionsComponent` com carregamento único do mapa `categoryId → name` em `ngOnInit`, fallback explícito para `categoryId` não encontrado no mapa e para falha na chamada a `GET /api/category` (listagem de transações não deve travar). Fora de escopo: mensagem de sucesso, breakpoints, cor de avatar, CSS compartilhado, seleção de `categoryId` no formulário. Ticket movido para fase `Spec`.
- 2026-08-03: UX-2 (Formatação de valores monetários e exibição do nome da categoria na listagem de Transações) implementado pelo `dev-frontend` em TDD a partir da spec: `CategoryService` injetado em `transactions.component.ts`, novo `loadCategories()` (chamado junto com `loadTransactions()` em `ngOnInit`) monta um mapa `categoryId → name` a partir de `GET /api/category`, com falha tolerada silenciosamente (sem estado de erro dedicado, conforme spec); novo método `categoryName(transaction)` resolve o nome, retorna `null` se `categoryId` ausente, ou o fallback `'Categoria não encontrada'` se presente mas não encontrado no mapa (mesmo fallback usado quando o carregamento de categorias falha). Template (`transactions.component.html`) passou a usar `CurrencyPipe` (`| currency:'BRL'`, sinal fora do pipe) e `DatePipe` (`| date:'dd/MM/yyyy'`). `app.config.ts` ganhou registro de locale `pt-BR` (`registerLocaleData` + `LOCALE_ID`), necessário para o formato `R$ 1.234,50` dos pipes. 10 novos testes (`UX-2 criterio 1-10`); 2 asserções pré-existentes que checavam valor cru (`'5000'`/`'150'`) foram atualizadas para o valor formatado esperado, já que contradiziam o novo comportamento exigido pela spec. `CategoryService`, `category.model.ts` e a tela de Categorias não foram tocados. Suíte 96/96, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-03: UX-2 (Formatação de valores monetários e exibição do nome da categoria na listagem de Transações) aprovado pelo `qa` — validação independente confirmou os 10 critérios de aceite direto em `transactions.component.ts/html`: `CurrencyPipe`/`DatePipe` corretos, sinal fora do pipe, distinção correta entre `categoryId` ausente (nenhum texto) e não encontrado/falha de carregamento (fallback), `GET /api/category` chamado uma única vez, falha na chamada de categoria não bloqueia a listagem de transações. QA encontrou uma lacuna de cobertura (nenhum teste do dev cobria diretamente o separador `' · '` só aparecer quando categoria e data estão ambos presentes) e adicionou 4 testes ele mesmo, todos passando sem necessidade de alteração de produção — confirmando que o comportamento já estava correto, só faltava teste. Suíte final 100/100, build limpo. **UX-2 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-03: Spec do UX-3 (Logout na NavBar) escrita pelo `po` em `docs/specs/ux-3-logout.md`. 6 critérios de aceite: botão "Sair" visível na `NavBarComponent`, novo método `AuthService.logout()` que remove o token da chave `TOKEN_STORAGE_KEY` (`'auth_token'`) sem chamada de rede, `getToken()` retorna `null` após logout, navegação para `/login` pós-logout, requisição subsequente sem header `Authorization` (validando o comportamento já existente do `authInterceptor`, sem exigir alteração nele), botão "Sair" ausente em `/` e `/login`. Fora de escopo: "Olá, {firstName}", guard de rota protegida (limitação conhecida documentada), endpoint de logout (não existe no contrato), diálogo de confirmação. Ticket movido para fase `Spec`.
- 2026-08-03: UX-3 (Logout na NavBar) implementado pelo `dev-frontend` em TDD a partir da spec: novo método `AuthService.logout()` remove o token via `localStorage.removeItem(TOKEN_STORAGE_KEY)` (síncrono, sem chamada de rede); `NavBarComponent` ganhou botão "Sair" (`<button type="button" (click)="onLogout()">`) que chama `logout()` e navega para `/login` via `Router.navigateByUrl`. Critério 5 (ausência do header `Authorization` pós-logout) já coberto por teste pré-existente do interceptor, sem necessidade de alterá-lo. Nenhum guard de rota criado (fora de escopo). 5 novos testes (2 em `auth.service.spec.ts`, 3 em `nav-bar.component.spec.ts`). Suíte 105/105, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-03: UX-3 (Logout na NavBar) aprovado pelo `qa` — validação independente confirmou os 6 critérios de aceite direto em `auth.service.ts`/`nav-bar.component.ts/html`. QA encontrou uma lacuna real de cobertura no critério 5 (header `Authorization` ausente pós-logout): o teste pré-existente citado pelo dev usava `AuthService` mockado, não exercitando o fluxo real `logout() → interceptor`; QA adicionou um teste de integração real em `auth.interceptor.spec.ts` (`AuthService` real, sem mock) fechando a lacuna, sem necessidade de retrabalho do dev. Suíte final 106/106, build limpo. **UX-3 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-03: Spec do UX-4 (Extrair estilos compartilhados) escrita pelo `po` em `docs/specs/ux-4-estilos-compartilhados.md`. Abordagem técnica escolhida: mixins SCSS em novo partial `src/styles/_shared.scss`, consumidos via `@use` em `transactions.component.scss`, `categories.component.scss` e `login.component.scss` (a spec identificou que a duplicação real vai além dos 3 blocos citados na revisão de UX — inclui também `.field`, `.field label`, `.field-error`/`.form-error` e o contêiner de formulário; e que `.login-card` não é idêntico a `.card`, exigindo uma camada base comum + propriedades locais por tela). Critérios de aceite: fonte única para os blocos duplicados, zero mudança de classe/estrutura no DOM (nenhum `.html` tocado), suíte 106/106 verde sem editar `.spec.ts` além do estritamente necessário, build limpo sem aumento inesperado de bundle, nenhuma regressão visual. Fora de escopo: mudança visual nova, tokens de tipografia/espaçamento e breakpoints (achados #10 e #7, não ticketados nesta rodada). Ticket movido para fase `Spec`.
- 2026-08-03: UX-4 (Extrair estilos compartilhados) implementado pelo `dev-frontend`: novo partial `src/styles/_shared.scss` com 6 mixins (`mm-card-base`, `mm-form-stack`, `mm-field-group`, `mm-form-field-input`, `mm-field-error`, `mm-pill-button`), consumidos via `@use` em `transactions.component.scss`, `categories.component.scss` e `login.component.scss`; `.login-card` manteve suas propriedades locais divergentes (`max-width`, `padding`, layout flex) fora do mixin, conforme caso de borda previsto na spec. Nenhum `.html`/`.spec.ts` alterado. Build limpo, sem aumento de bundle de estilos. **Achado relevante fora do escopo deste ticket**: dev-frontend relatou uma falha intermitente (flaky, dependente de ordem de execução do Jasmine) em `auth.interceptor.spec.ts`, no bloco `criterio 5 (ux-3-logout)` adicionado pelo `qa` durante o UX-3 — quando esse describe roda antes de outro `it` do describe externo configurar `httpMock`, o `afterEach` externo quebra (`Cannot read properties of undefined (reading 'verify')`). Não é causado pelo refactor de CSS; reproduz independente do UX-4. Ticket movido para fase `Em QA`, com essa questão de isolamento de teste repassada ao `qa` para investigação/correção do arquivo de spec (não é código de produção).
- 2026-08-03: UX-4 (Extrair estilos compartilhados) aprovado pelo `qa` — validação independente confirmou os 5 critérios de aceite via grep/diff direto no código: os 3 blocos de propriedades literais duplicadas (`background: var(--mm-color-surface)`, `border-radius: var(--mm-radius-card)`, `box-shadow: var(--mm-shadow-card)`) têm zero ocorrências fora de `src/styles/_shared.scss` — a duplicação foi de fato eliminada, não só encapsulada; nenhuma classe/seletor do DOM mudou; nenhum valor computado de CSS mudou (`git diff` conferido propriedade a propriedade); `.login-card` manteve suas propriedades locais divergentes fora do mixin. **Achado adicional corrigido pelo QA, fora do escopo do UX-4**: o teste flaky em `auth.interceptor.spec.ts` (relatado pelo dev-frontend, causado pelo describe `criterio 5 (ux-3-logout)` do próprio QA no UX-3 rodando fora de ordem e deixando `httpMock` do describe externo `undefined`) foi reproduzido (falhava em ~3 de 8 execuções) e corrigido pelo QA com um guard de nulidade no `afterEach` externo — 12/12 execuções consecutivas estáveis após a correção, nenhuma alteração de código de produção. Suíte final 106/106, build limpo. **UX-4 concluído — Done, sem nenhuma rodada de rejeição.** Com isso, todos os 4 tickets derivados da revisão de UX/UI (`docs/design/ux-review-2026-08-03.md`) estão concluídos: UX-1, UX-2, UX-3 e UX-4, todos aprovados de primeira.
- 2026-08-03: Spec do MVP-4 (Cartões de Crédito) escrita pelo `po` em `docs/specs/mvp-4-cartoes-credito.md`, cobrindo `GET/POST /api/creditcard` com os DTOs `CreditCardRequest`/`CreditCardResponse`/`CreditCardResponseList` e o enum `CardBrand` (numérico) do contrato. 14 critérios de aceite (listagem: sucesso, vazio, erro de rede; criação: sucesso com e sem `creditLimit` opcional, validações client-side de `name`, `brand`, `lastFourDigits` — tamanho exato de 4 caracteres e regra adicional de "somente dígitos" explicitamente marcada na spec como não-literal-do-contrato —, `closingDay`/`dueDay` 1-28, erro 400, erro de rede). Contrato de Credit Cards em `docs/api-contract.md` conferido como completo e coerente para o escopo listar+criar, sem lacunas. MVP-5 (compras/fatura) e edição/exclusão de cartão ficaram explicitamente fora de escopo. Ticket movido para fase `Spec`.
- 2026-08-04: MVP-4 (Cartões de Crédito) implementado pelo `dev-frontend` a partir da spec: `src/app/core/models/credit-card.model.ts` (`CreditCardRequest`/`CreditCardResponse`/`CreditCardResponseList`, mais o enum numérico `CardBrand` adicionado em `enums.model.ts`); `src/app/core/services/credit-card.service.ts` (getAll/create via `environment.apiBaseUrl`) com `credit-card.service.spec.ts`; `src/app/features/credit-cards/credit-cards.component.ts/.html/.scss` (listagem + formulário de criação, validadores customizados `lastFourDigitsValidator`/`dayRangeValidator`, payload omitindo `creditLimit` quando não preenchido); rota `credit-cards` adicionada em `app.routes.ts`; link "Cartões" adicionado à `NavBarComponent`. 132/132 testes, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-04: MVP-4 (Cartões de Crédito) aprovado pelo `qa` — validação independente dos 14 critérios de aceite e casos de borda direto no código: `CreditCardRequest`/`CreditCardResponse`/`CreditCardResponseList` batem exatamente com o contrato, `CardBrand` sempre numérico (`0`–`4`, nunca string) tanto no `<select>` (`[ngValue]`) quanto no payload enviado e nas comparações/labels PT-BR, `creditCardId` sempre `number` (nunca usado como string em `track`/comparações), `creditLimit` omitido do payload (não `null`/`undefined` explícito) quando o usuário deixa o campo em branco, `lastFourDigits` sempre enviado como `string` mesmo com a validação adicional de "somente dígitos", erro 400 e erro de rede na criação geram mensagens distintas mantendo o formulário preenchido e sem adicionar cartão à listagem, erro de rede na listagem distinto do estado vazio. `enums.model.ts`/`app.routes.ts` só receberam adição aditiva (`CardBrand`, rota `credit-cards`), sem alteração dos enums/rotas preexistentes — sem regressão. API real (`https://localhost:7002/swagger/v1/swagger.json`) indisponível no momento da validação do QA (mesma limitação conhecida registrada no histórico de 2026-08-03), então a confirmação do payload real ficou restrita à checagem já feita pelo dev-frontend nesta sessão — risco residual já aceito nos tickets anteriores. QA encontrou uma lacuna de cobertura (nenhum teste confirmava que `creditLimit` ausente na resposta não é exibido na linha do cartão na listagem, critério de aceite 1) e adicionou 1 teste ele mesmo, sem necessidade de retrabalho do dev. Suíte final 133/133, build limpo. **MVP-4 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-04: Novo ticket aberto a pedido do usuário — hoje, ao criar uma transação, não há como selecionar `categoryId` no formulário (`TransactionsComponent`), só a exibição do nome da categoria na listagem foi entregue (UX-2). Batizado **MVP-6** em vez de um novo `UX-N`: os tickets `UX-*` existentes derivam exclusivamente de achados documentados em `docs/design/ux-review-2026-08-03.md`, e este ticket não vem de uma revisão de design — é uma lacuna funcional de escopo, já registrada desde o histórico do MVP-2 ("`categoryId` deixado fora da UI de propósito... depende do MVP-3, ainda Backlog" — dependência já resolvida, MVP-3 está Done). `MVP-5` já está reservado (Compras no cartão + Fatura, Backlog), então o próximo ID sequencial de MVP é `MVP-6`. Sem endpoint novo — `categoryId` já existe em `TransactionRequest`/`TransactionResponse` no contrato, e `CategoryService`/`GET /api/category` já são consumidos desde UX-2 (só para exibição, não para seleção). Ticket adicionado a `docs/board.md` em fase `Backlog`.
- 2026-08-04: Spec do MVP-6 (Seleção de categoria na criação de Transações) escrita pelo `po` em `docs/specs/mvp-6-selecao-categoria-transacoes.md`, cobrindo `GET /api/category` (reaproveitando a chamada já existente em `TransactionsComponent.loadCategories()`, desde a UX-2 — sem segunda chamada) e `POST /api/transaction` (novo `categoryId` no payload quando selecionado). 8 critérios de aceite: opções do `<select>` populadas a partir de `CategoryResponse[]` com `[ngValue]` numérico, ausência de segunda chamada a `GET /api/category`, lista de categorias vazia, falha de carregamento de categorias (formulário de transação permanece funcional), inclusão de `categoryId` numérico no payload quando selecionado, omissão do campo quando não selecionado, erro 400 reaproveitando a mensagem genérica já existente desde o MVP-2, e ausência de `Validators.required` no novo campo. Decisão explícita documentada na spec: `categoryId` permanece **opcional** no formulário (contrato define `categoryId?: number`, sem regra de obrigatoriedade) — tornar obrigatório fica registrado como possível ticket futuro de produto, não implícito aqui. Reforça o padrão `[ngValue]` numérico para não reintroduzir o bug crítico de contrato (enums/`*Id` como string) já documentado no histórico. Fora de escopo: criar/editar/excluir categoria a partir do formulário de transação, filtrar select por `type`, qualquer mudança na tela de Categorias. Ticket movido para fase `Spec`.
- 2026-08-04: MVP-6 (Seleção de categoria na criação de Transações) implementado pelo `dev-frontend` em TDD estrito a partir da spec: `src/app/features/transactions/transactions.component.ts` ganhou a propriedade pública `categories: CategoryResponse[]` (populada na mesma chamada de `loadCategories()` já existente desde a UX-2 — nenhuma segunda chamada a `GET /api/category`), novo `FormControl<number | null>` `categoryId` (sem `Validators.required`, conforme decisão explícita da spec), método `categoryOptionLabel()` (rótulo `"{name} ({type traduzido})"` para desambiguar categorias homônimas, reaproveitando `transactionTypeLabels` já existente), `categoryId` incluído em `buildRequest()` apenas quando não-nulo (mesmo padrão de `paymentMethod`) e reset pós-sucesso agora zera também `categoryId`. `transactions.component.html` ganhou `<select id="category" formControlName="categoryId">` com `<option [ngValue]="null">Nenhuma</option>` como padrão e `<option [ngValue]="category.categoryId">` numérico para cada categoria — mesmo padrão `[ngValue]` já usado nos selects de `type`/`paymentMethod`, sem reintroduzir o bug de contrato de enum/`*Id`-como-string. 9 novos testes (`MVP-6 criterio 1` a `8`, mais um caso de borda de rótulo). `CategoryService`, `category.model.ts` e a tela de Categorias não foram tocados. Suíte 133 → 142/142 testes, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-04: MVP-6 (Seleção de categoria na criação de Transações) aprovado pelo `qa` — validação independente confirmou os 8 critérios de aceite direto no código (não no autorrelato do dev): `CategoryResponse`/`TransactionRequest.categoryId` batem exatamente com o contrato; `category.model.ts`, `category.service.ts` e a tela de Categorias não foram tocados (`git diff --stat` confere zero mudança); select de categoria usa `[ngValue]` numérico com opção padrão "Nenhuma"; `loadCategories()` continua sendo a única chamada a `GET /api/category` (populando tanto o mapa `categoryId → name` da UX-2 quanto a nova lista `categories` do MVP-6, sem chamada duplicada); lista vazia e falha de carregamento não travam o formulário de criação; payload de `POST /api/transaction` inclui `categoryId` numérico quando selecionado e omite a propriedade (não `null`/`0`) quando não selecionado; erro 400 reaproveita a mensagem genérica já existente desde o MVP-2, sem mensagem nova; `categoryId` sem `Validators.required`, conforme decisão explícita da spec; reset pós-sucesso volta `categoryId` a `null`. QA rodou a suíte e o build de forma independente — 142/142 testes, build limpo — e não encontrou nenhuma lacuna de cobertura que justificasse teste adicional (os 9 testes do dev já cobrem todos os critérios e o caso de borda do rótulo desambiguado). **MVP-6 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-04: Spec do MVP-5 (Compras no cartão + Fatura) escrita pelo `po` em `docs/specs/mvp-5-compras-cartao-fatura.md`, cobrindo `POST /api/creditcard/{creditCardId}/purchases` (`CreditCardPurchaseRequest` → `CreditCardInstallmentResponse`) e `GET /api/creditcard/{creditCardId}/fatura?month=&year=` (→ `CreditCardInvoiceResponse` contendo `installments: CreditCardInstallmentResponse[]`), reutilizando `CreditCardService.getAll()` (MVP-4) para obter o `creditCardId` de origem. 16 critérios de aceite (10 de compra: sucesso com/sem opcionais, `isSubscription` sempre enviado explicitamente, validações client-side de `description`/`totalValue`/`installmentsCount`, erro 400/rede, a resposta do POST — um único `CreditCardInstallmentResponse` mesmo com `installmentsCount > 1` — não pode ser tratada como lista completa de parcelas; 6 de fatura: carregamento com sucesso/vazio/erro, navegação sequencial mês anterior/próximo mês com rollover de ano, erro 401 genérico). Decisões de escopo principais: nova rota `credit-cards/:creditCardId` com um novo componente de detalhe (`CreditCardDetailComponent`), acessada via link "Ver fatura" adicionado a cada item da listagem do MVP-4 (a listagem em si não muda de comportamento); cada parcela de `installments[]` é renderizada como linha independente, sem agrupamento por compra (o contrato não fornece um identificador de compra compartilhado entre parcelas); navegação de fatura limitada a mês anterior/próximo mês sequenciais (sem seletor livre de data), calculada sempre a partir de `invoiceMonth`/`invoiceYear` da última resposta `200` recebida; `categoryId` opcional na compra reaproveita `CategoryService`/`GET /api/category` no mesmo padrão `[ngValue]` numérico do MVP-6. Sem campos enum neste ticket. Ticket movido para fase `Spec`.
- 2026-08-04: MVP-5 (Compras no cartão + Fatura) implementado pelo `dev-frontend` em TDD estrito a partir da spec: `src/app/core/models/credit-card.model.ts` ganhou `CreditCardPurchaseRequest`/`CreditCardInstallmentResponse`/`CreditCardInvoiceResponse` (sem nenhum campo enum, todos os `*Id`/numéricos como `number`); `src/app/core/services/credit-card.service.ts` ganhou `createPurchase(creditCardId, request)` (`POST /api/creditcard/{id}/purchases`) e `getInvoice(creditCardId, month?, year?)` (`GET /api/creditcard/{id}/fatura`, `HttpParams` só incluindo `month`/`year` quando informados) — 6 novos testes em `credit-card.service.spec.ts`; novo `src/app/features/credit-cards/credit-card-detail.component.ts/.html/.scss` (rota `credit-cards/:creditCardId` registrada em `app.routes.ts`) implementa formulário de compra (validadores customizados `positiveValueValidator`/`installmentsCountRangeValidator`, payload omitindo `purchaseDate`/`installmentsCount`/`categoryId` quando não preenchidos, `isSubscription` sempre enviado explicitamente) e exibição/navegação de fatura (`shiftMonth()` privado calcula mês anterior/seguinte com rollover de ano a partir de `invoiceMonth`/`invoiceYear` da última resposta; erro de navegação preserva a fatura anterior como referência) — 23 novos testes em `credit-card-detail.component.spec.ts`, cobrindo os 16 critérios de aceite mais casos de borda (rollover, resposta do POST não usada como lista completa, 401 tratado igual a erro de rede/carregamento). `credit-cards.component.html/.ts` ganhou link "Ver fatura" por item (`RouterLink` para `/credit-cards/:creditCardId`) — 1 novo teste em `credit-cards.component.spec.ts`; nenhuma outra mudança de comportamento na listagem. `creditCardId` do parâmetro de rota convertido explicitamente para `number` (`Number(route.snapshot.paramMap.get('creditCardId'))`), não reintroduzindo o bug histórico de contrato `*Id`-como-string. Suíte 142 → 172/172 testes, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-04: MVP-5 (Compras no cartão + Fatura) aprovado pelo `qa` — validação independente dos 16 critérios de aceite direto no código (não no autorrelato do dev-frontend): `CreditCardPurchaseRequest`/`CreditCardInstallmentResponse`/`CreditCardInvoiceResponse` batem exatamente com `docs/api-contract.md` (nenhum campo enum inventado, todo `*Id`/numérico como `number`); `creditCardId` da rota Angular convertido explicitamente via `Number(this.route.snapshot.paramMap.get('creditCardId'))`, usado consistentemente como `number` em payload/comparações/`getInvoice`; `isSubscription` sempre presente no payload (`raw.isSubscription` direto, nunca condicional); `purchaseDate`/`installmentsCount`/`categoryId` omitidos (não `null`) do payload quando em branco, confirmado via asserção `'chave' in sentRequest` nos testes; a resposta única do `POST .../purchases` nunca é usada como lista de parcelas — a lista exibida vem exclusivamente da chamada subsequente a `GET .../fatura` (critério 10 testado explicitamente); navegação mês anterior/próximo mês calcula `month`/`year` a partir de `invoiceMonth`/`invoiceYear` da última resposta `200`, com rollover de ano correto em ambas as direções, e preserva a fatura anterior como referência quando uma navegação falha; erro `401` em ambos os endpoints cai no mesmo tratamento genérico já usado para erro de rede (mesma mensagem, sem redirecionamento), confirmado com testes que comparam a mensagem de 401 à de erro `status: 0`; `CreditCardsComponent` (MVP-4, já `Done`) só recebeu o link aditivo "Ver fatura" (`RouterLink`) — `git diff` conferido linha a linha confirma zero mudança de comportamento na listagem/formulário existentes, sem regressão. QA rodou a suíte e o build de forma independente — 172/172 testes, build limpo — e não encontrou nenhuma lacuna de cobertura que justificasse teste adicional; os 23 testes de `credit-card-detail.component.spec.ts` mais os 6 de `credit-card.service.spec.ts` já cobrem todos os critérios e os casos de borda relevantes (rollover em ambas direções, resposta do POST não tratada como lista completa, seleção de categoria numérica). **MVP-5 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-04: Revisão de UX/UI gerada pelo agente `designer` em `docs/design/ux-review-2026-08-04.md`, cobrindo as telas novas do MVP-5 (`credit-card-detail`) e revisão de consistência com Cartões/Transações/Categorias/Login/NavBar já `Done`. Achado central: zero `@media query` em todo o projeto — as 4 telas de conteúdo (`transactions`, `categories`, `credit-cards`, `credit-card-detail`) repetem, byte a byte, o mesmo contêiner de página com `max-width: 480px` fixo, desperdiçando ~400-480px de espaço em telas largas e empilhando lista+formulário mesmo quando já coexistem na mesma tela (achado #1, Alta severidade, agravamento do achado #7 já registrado em 2026-08-03 e nunca ticketado). Usuário aprovou explicitamente abrir dois tickets a partir desta revisão: **UX-5** (responsividade desktop, achados #1/#7 — prioridade máxima desta rodada) e **UX-6** (identificação do cartão/breadcrumb na fatura, `CurrencyPipe`/`DatePipe` na fatura e no limite do cartão, hierarquia tabular das parcelas — achados #2/#3/#4, a ser especificado em seguida). Os achados de menor prioridade não pedidos pelo usuário nesta rodada (#5 `flex-wrap` do nav de mês, #6 mensagem de sucesso, #9 opção "Selecione" desabilitada, #10 cor por bandeira) permanecem apenas registrados no documento de revisão, sem ticket. Ambos os tickets adicionados a `docs/board.md`.
- 2026-08-04: Spec do UX-5 (Responsividade desktop e extração do page shell compartilhado) escrita pelo `po` em `docs/specs/ux-5-responsividade-desktop.md`. Sem endpoint/DTO envolvido — ticket 100% CSS/SCSS. Abordagem técnica: dois mixins novos em `src/styles/_shared.scss` (mesmo partial do UX-4) — `mm-page-shell($desktop-max-width: 1180px, $desktop-breakpoint: 960px)`, substituindo o contêiner de página hoje duplicado nas 4 telas e acrescentando, a partir de `960px`, `display: grid; grid-template-columns: 1.4fr 1fr; gap: 1.5rem;` para colocar lista/fatura (coluna maior) e formulário (coluna menor) lado a lado; e `mm-card-width($max-width: 480px, $desktop-breakpoint: 960px)`, extraindo o `max-width: 480px` hoje duplicado no `.card` das 4 telas e relaxando esse limite no mesmo breakpoint, para o card preencher a coluna do grid. Decisão explícita de **não** estender `mm-card-base` (UX-4) nem tocar `login.component.scss`/`login.component.html` — a tela de login está fora do escopo dos achados #1/#7 e seu `.login-card` já diverge intencionalmente em dimensões desde o UX-4. Investigação da estrutura de DOM das 4 telas confirmou que os dois `<section class="... card">` de cada página já são filhos diretos do contêiner — `display: grid` aplicado diretamente ao contêiner já basta para o layout lado a lado, então nenhuma mudança de `.html` é esperada por padrão; a spec documenta explicitamente (critério de aceite 8) sob quais condições uma mudança de `.html` seria excepcionalmente aceitável, caso o `dev-frontend` identifique necessidade real durante a implementação. 10 critérios de aceite cobrindo: fonte única dos dois blocos extraídos, breakpoint concreto (`960px`), preservação do comportamento mobile como caso base, grid lado a lado nas 3 telas lista+formulário e na tela de detalhe do cartão, relaxamento do `max-width` do card em desktop, zero regressão de DOM/classes testadas (172 testes), suíte e build verdes, e confirmação de que `login.component.scss` não é tocado. Seção "Referência visual" citando `docs/design/mockup.pdf` pág. 1 registrada com nota de limitação de ambiente (leitura direta do PDF falhou por `poppler-utils` ausente nesta sessão — spec se apoiou na descrição já validada pelo `designer` na revisão de origem). Ticket movido para fase `Spec`.
- 2026-08-04: UX-5 (Responsividade desktop e extração do page shell compartilhado) implementado pelo `dev-frontend` a partir da spec: dois mixins novos em `src/styles/_shared.scss` — `mm-page-shell($desktop-max-width: 1180px, $desktop-breakpoint: 960px)` (substitui o bloco `min-height:100vh; display:flex; flex-direction:column; gap:1.5rem; padding:2rem 1rem; background: var(--mm-color-bg); box-sizing:border-box;` hoje duplicado nos 4 `.component.scss`, acrescentando `@media (min-width: 960px)` que amplia o contêiner e troca `flex-direction: column` por `display: grid; grid-template-columns: 1.4fr 1fr; gap: 1.5rem; align-items: start;`) e `mm-card-width($max-width: 480px, $desktop-breakpoint: 960px)` (extrai `width:100%; max-width:480px; margin:0 auto;` do `.card`, relaxando para `max-width:none; margin:0;` no mesmo breakpoint). Aplicados via `@include` em `transactions.component.scss`, `categories.component.scss`, `credit-cards.component.scss` e `credit-card-detail.component.scss` — `.xxx-page` inclui `mm-page-shell`; `.card` inclui `mm-card-base` (UX-4) + `padding: 1.75rem 1.5rem` local + `mm-card-width`, nessa ordem específica para evitar warning de Sass `@media mixed-decls` (divergência de ordem em relação ao snippet ilustrativo da spec, sem violar nenhum dos 10 critérios literais). Nenhum `.html` alterado — os dois `<section class="... card">` de cada tela já eram filhos diretos do contêiner de página, então `display: grid` no seletor de página já bastou (critério de aceite 8, sem necessidade do wrapper opcional previsto como salvaguarda). Nenhum `.spec.ts` alterado. `login.component.scss`/`.html` não tocados. Suíte 172/172 (inalterada), build limpo. Ticket movido para fase `Em QA`.
- 2026-08-04: UX-5 (Responsividade desktop e extração do page shell compartilhado) aprovado pelo `qa` — validação independente dos 10 critérios de aceite direto no código (não no autorrelato do dev-frontend): `git diff` confirmou que os dois blocos literais antigos (`min-height:100vh;...` do contêiner de página e `width:100%; max-width:480px; margin:0 auto;` do `.card`) foram removidos byte a byte dos 4 arquivos e substituídos por `@include shared.mm-page-shell`/`@include shared.mm-card-width`, com o caso base (mobile/tablet) idêntico ao estado anterior; breakpoint confirmado exatamente `@media (min-width: 960px)` em `_shared.scss`, grid `1.4fr 1fr` com `gap: 1.5rem` e `align-items: start`; os 4 `.html` inspecionados confirmam 2 filhos diretos `<section class="... card">` por página, na ordem lista/fatura → formulário, sem wrapper novo — grep confirmou zero ocorrência de `min-height: 100vh`/`max-width: 480px` fora de `_shared.scss` (a única outra ocorrência de `max-width`/`min-height` no `src/` é `login.component.scss`, com valor divergente `380px`, não tocado); `git diff`/`git status` confirmaram `login.component.scss` e `login.component.html` sem nenhuma alteração; nenhum `.html` tocado por este ticket (o único diff de `credit-cards.component.html` no working tree é o link "Ver fatura" do MVP-5, não relacionado a UX-5); nenhuma classe usada em asserção de teste renomeada/removida. QA rodou a suíte e o build de forma independente — 172/172 testes, build limpo, sem warnings novos (incluindo nenhum warning de Sass `@media mixed-decls`, validando a ordem de `@include` escolhida pelo dev). Não foi encontrada lacuna de cobertura que justificasse teste adicional — a spec já documenta explicitamente que este ticket é validável inteiramente por inspeção de código/DOM, sem exigir teste automatizado de breakpoint (Karma/Jasmine não simula `window.innerWidth`/media queries de forma confiável neste projeto). **UX-5 concluído — Done, sem nenhuma rodada de rejeição.**
- 2026-08-04: Spec do UX-6 (Fatura do cartão: identificação, formatação de moeda/data e hierarquia de parcelas) escrita pelo `po` em `docs/specs/ux-6-fatura-cartao-polish.md`. Sem endpoint/DTO novo — conferido contra `docs/api-contract.md` (só campos já existentes de `CreditCardResponse`/`CreditCardInvoiceResponse`/`CreditCardInstallmentResponse`; confirmado que **não existe** endpoint singular `GET /api/creditcard/{id}`, só a listagem). Três decisões técnicas explícitas: (1) identificação do cartão via `CreditCardService.getAll()` (já existente) + filtro client-side por `creditCardId` da rota — não `getById`, que exigiria endpoint inexistente; nova propriedade `creditCard: CreditCardResponse | null` em `CreditCardDetailComponent`, falha/não-encontrado tratados de forma não bloqueante (fallback ao título genérico "Fatura"); (2) `CardBrand` label duplicado localmente em `credit-card-detail.component.ts` (mesmo padrão já aceito no projeto de constantes de rótulo por componente, sem extração compartilhada); (3) hierarquia tabular das parcelas via `display: grid` (não `<table>`) — cabeçalho novo `.installment-list__header` visível só a partir do breakpoint `min-width: 960px` já existente (UX-5), com `grid-template-columns` idêntico compartilhado entre cabeçalho e `.installment-item`; abaixo de 960px, mínimo exigido é `font-weight: 700` no valor da parcela. 14 critérios de aceite (4 de identificação/link de volta, 4 de `CurrencyPipe`/`DatePipe` na fatura e no `creditLimit` da listagem, 5 de hierarquia tabular, 1 de `flex-wrap` em `.invoice__nav` — achado #5, incluído por decisão explícita do `po` por ser custo marginal trivial, já cotado como candidato na spec do UX-5). Fora de escopo: achados #6/#8/#9/#10 (o #8, nome do mês por extenso, avaliado e descartado explicitamente por não ser coberto de graça pela formatação de pipe já decidida — exigiria transformação nova de `invoiceMonth`/`invoiceYear` em `Date`), ARIA completa de tabela, extração de labels de enum compartilhados entre componentes, endpoint singular de cartão por id. `docs/design/mockup.pdf` segue sem página dedicada a Cartões/Fatura (mesma limitação de ambiente para leitura de PDF já registrada no UX-5) — layout fino (título, link de volta, cabeçalho de colunas) fica a critério do `dev-frontend`. Ticket movido para fase `Spec`.
- 2026-08-04: UX-6 (Fatura do cartão: identificação, formatação de moeda/data e hierarquia de parcelas) implementado pelo `dev-frontend` a partir da spec: `credit-card-detail.component.ts` ganhou `loadCreditCard()` (chamado em `ngOnInit()` junto de `loadInvoice()`/`loadCategories()`), reaproveitando `CreditCardService.getAll()` já existente (nenhum método novo no service, nenhum endpoint novo) + `find(c => c.creditCardId === this.creditCardId)`, resultado em `creditCard: CreditCardResponse | null`; falha ou "não encontrado" tratados de forma silenciosa/não bloqueante (título cai no fallback "Fatura"); `CARD_BRAND_LABELS` duplicado localmente (mesmas strings PT-BR de `credit-cards.component.ts`, decisão explícita da spec de não extrair label compartilhado). `credit-card-detail.component.html` ganhou link `routerLink="/credit-cards"` sempre presente no DOM independente do estado, título condicional (nome/bandeira/últimos 4 dígitos ou fallback genérico), `CurrencyPipe`/`DatePipe` em `totalValue`/`dueDate`/`installment.value`/`installment.purchaseDate` (nenhum valor cru no DOM), `invoiceMonth`/`invoiceYear` deliberadamente intocados (fora de escopo, achado #8), e novo `.installment-list__header` (4 rótulos "Descrição"/"Parcela"/"Valor"/"Data", renderizado só quando há parcelas, imediatamente antes de `.installment-list`). `credit-card-detail.component.scss` ganhou `.invoice__back-link`, `flex-wrap: wrap` em `.invoice__nav`, e a variável `$installment-grid-columns` compartilhada (nunca redeclarada) entre `.installment-list__header` e `.installment-item` dentro de `@media (min-width: 960px)` (breakpoint reaproveitado do UX-5, não recriado), mais `.installment-item__value { font-weight: 700; }` como caso base (< 960px). `credit-cards.component.html/.ts` ganharam `CurrencyPipe` em `creditLimit`, preservando a condição `@if` existente. 13 novos testes em `credit-card-detail.component.spec.ts` (2 pré-existentes ajustados para valores formatados) e 1 novo em `credit-cards.component.spec.ts` (1 pré-existente ajustado); dev relatou explicitamente não ter escrito teste automatizado para os aspectos de CSS gated por media query (grid ativo ≥960px, igualdade exata de `grid-template-columns`), mesmo precedente já aceito no UX-5 (Karma roda abaixo de 960px, sem `matchMedia`/`getComputedStyle` cross-breakpoint), mitigado usando uma única variável SCSS compartilhada em vez de valores redeclarados. Suíte 172 → 185/185 testes, build limpo. Ticket movido para fase `Em QA`.
- 2026-08-04: UX-6 (Fatura do cartão: identificação, formatação de moeda/data e hierarquia de parcelas) aprovado pelo `qa` — validação independente dos 14 critérios de aceite direto no código (não no autorrelato do dev-frontend): confirmado via `git diff` que `CreditCardService`/`credit-card.model.ts`/`category.model.ts` não sofreram nenhuma mudança de contrato (os diffs existentes no working tree para esses arquivos são leftovers não commitados do MVP-5, anteriores a este ticket, não tocados por UX-6); `loadCreditCard()` de fato reaproveita `getAll()` sem método/endpoint novo, falha e "não encontrado" caem no mesmo fallback silencioso "Fatura" sem bloquear fatura/formulário (critérios 1-3); link de volta (`a[routerLink="/credit-cards"]`) sempre presente no DOM, inclusive em estado de erro de fatura (critério 4, teste dedicado confirma isso mesmo com `getInvoice` falhando); `CurrencyPipe`/`DatePipe` aplicados exatamente onde a spec pede — nenhum valor cru (`139.9`, `2026-08-17`) sobrando no DOM, `invoiceMonth`/`invoiceYear` seguem no formato numérico "8/2026" sem pipe (critérios 5, 6, 8); `creditLimit` da listagem com `CurrencyPipe`, condição `@if (creditCard.creditLimit != null)` preservada byte a byte (`git diff` confere, critério 7); cabeçalho `.installment-list__header` com os 4 rótulos na ordem certa, precedendo `.installment-list`, só renderizado quando há parcelas, `display: none` confirmado via `getComputedStyle` na viewport padrão do Karma (< 960px, critérios 9/10/13); `.installment-item__value` com `font-weight: 700` confirmado via `getComputedStyle` (critério 11); `.invoice__nav` com `flex-wrap: wrap` confirmado via `getComputedStyle` (critério 14). Para o critério 12 (igualdade exata de `grid-template-columns` entre `.installment-list__header` e `.installment-item` em `@media (min-width: 960px)`) — não coberto por teste automatizado pelo dev, mesma limitação já aceita no UX-5 (Karma não simula `window.innerWidth`/media queries de forma confiável neste projeto) — o QA validou de forma independente compilando o SCSS diretamente (`npx sass`) e confirmando que ambos os seletores geram, dentro do mesmo bloco `@media (min-width: 960px)`, exatamente `grid-template-columns: 2.2fr 0.9fr 1fr 1.1fr;`, a partir de uma única variável SCSS compartilhada (`$installment-grid-columns`) nunca redeclarada — evidência mais forte que um teste de runtime, sem alterar a configuração de lançamento do Karma/Chrome. QA rodou a suíte e o build de forma independente — 185/185 testes, build limpo — e não encontrou lacuna de cobertura que justificasse teste adicional; os 13 testes novos de `credit-card-detail.component.spec.ts` mais o 1 de `credit-cards.component.spec.ts` já cobrem todos os critérios verificáveis em runtime. **UX-6 concluído — Done, sem nenhuma rodada de rejeição.**
